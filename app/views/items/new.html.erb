<% if @item.errors.any? %>
	<%= render "shared/errors", object: @item %>
<% end %>

<%= form_for(@item) do |f| %>
	
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :number %>
	<%= f.number_field :number %>

	<%= f.submit "Add Item" %>
<% end %>